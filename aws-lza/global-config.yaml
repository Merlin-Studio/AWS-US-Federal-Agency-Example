# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# -----------------------------------------------------------------------------
# Generated by Merlin Studio v2 (architex-v2) — AWS Landing Zone Accelerator 1.14.x — global-config.yaml
# Organization: Acme Agency
# Profile:      ADVANCED
# Generated:    2026-06-13T13:27:14.177742Z
# Wizard:       v1.0.0
# -----------------------------------------------------------------------------
# Scaffolding — not a finished artifact. Merlin renders this from your
# declared intent (profile, frameworks, accounts, topology). Adapt it to
# your environment: tighten CIDRs, refine policy bodies, add team tags,
# pin module versions, layer account-specific overrides. Re-run the wizard
# when *intent* shifts; keep day-to-day customisation in your IaC fork
# (regen overwrites this directory).
# Replace PLACEHOLDER_ tokens with real values from your AWS Organization
# before `tofu apply` / `cdk deploy` / LZA pipeline run — see PLACEHOLDERS.md.
# Compliance frameworks declared on this spec:
#   - fedramp_moderate
#   - fedramp_high
#   - nist_800_53

# Global Landing Zone Accelerator configuration.
# Schema reference: https://awslabs.github.io/landing-zone-accelerator-on-aws/v1.14/typedocs/

homeRegion: us-west-1
enabledRegions:
  - us-east-1
  - us-east-2
  - us-west-1
  - us-west-2

managementAccountAccessRole: AWSControlTowerExecution
cloudwatchLogRetentionInDays: 2557

# Centralised logging configuration
centralizedLoggingRegion: us-west-1

logging:
  account: LogArchive
  cloudtrail:
    enable: true
    organizationTrail: true
    organizationTrailSettings:
      # Feature flags from generators/aws_cloudtrail_features.py so every
      # format ships the same trail config for a given compliance set.
      multiRegionTrail:    true
      globalServiceEvents: true
      managementEvents:    true
      s3DataEvents:        true
      lambdaDataEvents:    true
      sendToCloudWatchLogs: true
      apiErrorRateInsight: true
      apiCallRateInsight:  true
  sessionManager:
    sendToCloudWatchLogs: true
    sendToS3: true
    excludeRegions: []
    excludeAccounts: []
    lifecycleRules:
      - enabled: true
        abortIncompleteMultipartUpload: 7
        expiration: 730
        noncurrentVersionExpiration: 90
  accessLogBucket:
    lifecycleRules:
      - enabled: true
        abortIncompleteMultipartUpload: 7
        expiration: 730
        noncurrentVersionExpiration: 90
  centralLogBucket:
    lifecycleRules:
      - enabled: true
        abortIncompleteMultipartUpload: 7
        expiration: 2555  # central-log retention in days (default 2555 = 7y; compliance floors may raise it)
        noncurrentVersionExpiration: 90
    kmsResourcePolicyAttachments:
      - policy: kms/central-logs-key-policy.json
  elbLogBucket:
    lifecycleRules:
      - enabled: true
        abortIncompleteMultipartUpload: 7
        expiration: 365
        noncurrentVersionExpiration: 90

# Reports configuration
reports:
  costAndUsageReport:
    compression: Parquet
    format: Parquet
    reportName: acme-agency-cur
    s3Prefix: cost-and-usage-reports
    timeUnit: DAILY
    refreshClosedReports: true
    reportVersioning: OVERWRITE_REPORT
    lifecycleRules:
      - enabled: true
        abortIncompleteMultipartUpload: 7
        expiration: 2555
        noncurrentVersionExpiration: 90
  budgets:
    - name: org-monthly
      type: COST
      amount: 100000
      unit: USD
      includeUpfront: true
      includeTax: true
      includeSupport: true
      includeRecurring: true
      includeOtherSubscription: true
      includeSubscription: true
      includeDiscount: true
      includeRefund: false
      includeCredit: false
      useAmortized: false
      useBlended: false
      timeUnit: MONTHLY
      notifications:
        - type: ACTUAL
          thresholdType: PERCENTAGE
          threshold: 80
          comparisonOperator: GREATER_THAN
          subscriptionType: EMAIL
          address: "my3mail@acme.gov"
        - type: FORECASTED
          thresholdType: PERCENTAGE
          threshold: 100
          comparisonOperator: GREATER_THAN
          subscriptionType: EMAIL
          address: "my3mail@acme.gov"
      deploymentTargets:
        organizationalUnits:
          - Root
    - name: workloads-prod
      type: COST
      amount: 50000
      unit: USD
      includeUpfront: true
      includeTax: true
      includeSupport: true
      includeRecurring: true
      includeOtherSubscription: true
      includeSubscription: true
      includeDiscount: true
      includeRefund: false
      includeCredit: false
      useAmortized: false
      useBlended: false
      timeUnit: MONTHLY
      notifications:
        - type: ACTUAL
          thresholdType: PERCENTAGE
          threshold: 80
          comparisonOperator: GREATER_THAN
          subscriptionType: EMAIL
          address: "my3mail@acme.gov"
        - type: FORECASTED
          thresholdType: PERCENTAGE
          threshold: 100
          comparisonOperator: GREATER_THAN
          subscriptionType: EMAIL
          address: "my3mail@acme.gov"
      deploymentTargets:
        organizationalUnits:
          - Root
    - name: workloads-nonprod
      type: COST
      amount: 20000
      unit: USD
      includeUpfront: true
      includeTax: true
      includeSupport: true
      includeRecurring: true
      includeOtherSubscription: true
      includeSubscription: true
      includeDiscount: true
      includeRefund: false
      includeCredit: false
      useAmortized: false
      useBlended: false
      timeUnit: MONTHLY
      notifications:
        - type: ACTUAL
          thresholdType: PERCENTAGE
          threshold: 90
          comparisonOperator: GREATER_THAN
          subscriptionType: EMAIL
          address: "my3mail@acme.gov"
        - type: FORECASTED
          thresholdType: PERCENTAGE
          threshold: 110
          comparisonOperator: GREATER_THAN
          subscriptionType: EMAIL
          address: "my3mail@acme.gov"
      deploymentTargets:
        organizationalUnits:
          - Root

# AWS Control Tower integration
controlTower:
  enable: true
  landingZone:
    version: "3.3"
    logging:
      loggingBucketRetentionDays: 730
      accessLoggingBucketRetentionDays: 730
      organizationTrail: true
    security:
      enableIdentityCenterAccess: true

# AWS CDK Toolkit and Pipeline options
cdkOptions:
  centralizeBuckets: true
  useManagementAccessRole: true
  customDeploymentRole: ""
  forceBootstrap: false

# Optional: lockout time after pipeline approval
acceleratorMetadata:
  enable: true
  account: LogArchive
  readOnlyAccessRoleArns: []

# Tags applied to every accelerator-created resource
tags:
  - key: ManagedBy
    value: Merlin-Studio-v2
  - key: Organization
    value: Acme Agency
  - key: Profile
    value: advanced
  - key: Compliance
    value: "fedramp_moderate,fedramp_high,nist_800_53"
