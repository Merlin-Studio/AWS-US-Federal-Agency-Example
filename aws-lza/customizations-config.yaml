# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# -----------------------------------------------------------------------------
# Generated by Merlin Studio v2 (architex-v2) — LZA customizations-config.yaml
# Organization: Acme Agency
# Profile:      ADVANCED
# Generated:    2026-06-13T13:27:14.177742Z
# Wizard:       v1.0.0
# -----------------------------------------------------------------------------
# Scaffolding — not a finished artifact. Merlin renders this from your
# declared intent (profile, frameworks, accounts, topology). Adapt it to
# your environment: tighten CIDRs, refine policy bodies, add team tags,
# pin module versions, layer account-specific overrides. Re-run the wizard
# when *intent* shifts; keep day-to-day customisation in your IaC fork
# (regen overwrites this directory).
# Replace PLACEHOLDER_ tokens with real values from your AWS Organization
# before `tofu apply` / `cdk deploy` / LZA pipeline run — see PLACEHOLDERS.md.

# Optional LZA file — extends the accelerator with custom CloudFormation
# stacks, stack sets, Service Catalog portfolios, and custom applications.
# Schema: https://awslabs.github.io/landing-zone-accelerator-on-aws/v1.14/typedocs/

customizations:
  cloudFormationStacks:
    []

  cloudFormationStackSets:
    []

  serviceCatalogPortfolios:
    - name: approved-vpc-templates
      provider: Platform-Team
      account: SharedServices
      regions:
        - us-west-1
      shareTargets:
        organizationalUnits:
          - Workloads
      products:
        # Placeholder product. LZA rejects portfolios with an empty `products:`
        # list (schema requires >=1 entry), so when the wizard captured the
        # portfolio intent but no product yet, we emit a single stub product
        # tagged with PLACEHOLDER tokens. PLACEHOLDERS.md explains what to
        # replace with a real CloudFormation template path. The portfolio
        # still validates; nothing actually deploys until the operator wires
        # a real product.
        - name: PLACEHOLDER_PRODUCT_NAME_approved_vpc_templates
          description: Placeholder product — replace before LZA pipeline run.
          owner: Platform-Team
          versions:
            - name: v0.0.1
              description: Placeholder — wire a real CloudFormation template before deploy.
              template: PLACEHOLDER_PRODUCT_TEMPLATE_approved_vpc_templates

applications:
  []
