# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# -----------------------------------------------------------------------------
# Generated by Merlin Studio v2 (architex-v2) — LZA iam-config.yaml
# Organization: Acme Agency
# Profile:      ADVANCED
# Generated:    2026-06-13T13:27:14.177742Z
# Wizard:       v1.0.0
# -----------------------------------------------------------------------------
# Scaffolding — not a finished artifact. Merlin renders this from your
# declared intent (profile, frameworks, accounts, topology). Adapt it to
# your environment: tighten CIDRs, refine policy bodies, add team tags,
# pin module versions, layer account-specific overrides. Re-run the wizard
# when *intent* shifts; keep day-to-day customisation in your IaC fork
# (regen overwrites this directory).
# Replace PLACEHOLDER_ tokens with real values from your AWS Organization
# before `tofu apply` / `cdk deploy` / LZA pipeline run — see PLACEHOLDERS.md.

# IAM roles, IAM Identity Center, permission sets, and SAML/OIDC providers.
# Schema: https://awslabs.github.io/landing-zone-accelerator-on-aws/v1.14/typedocs/

providers:
  # OIDC identity provider (workload / CI federation).
  - name: GitHubActions
    metadataDocument: "https://token.actions.githubusercontent.com"

policySets:
  []

roleSets:
  - deploymentTargets:
      organizationalUnits:
        - Root
    roles:
      - name: BreakGlassRole
        assumedBy:
          - type: principalArn
            principal: arn:aws:iam::PLACEHOLDER_ACCOUNT_ID_Management:root
        policies:
          awsManaged:
            - AdministratorAccess
        boundaryPolicy: ''
      - name: BillingViewerRole
        assumedBy:
          - type: principalArn
            principal: arn:aws:iam::PLACEHOLDER_ACCOUNT_ID_Management:root
        policies:
          awsManaged:
            - job-function/Billing
        boundaryPolicy: ''
      - name: SecurityAuditorRole
        assumedBy:
          - type: principalArn
            principal: arn:aws:iam::PLACEHOLDER_ACCOUNT_ID_Audit:root
        policies:
          awsManaged:
            - SecurityAudit
            - ReadOnlyAccess
        boundaryPolicy: ''

groupSets:
  []

userSets:
  # IAM users are intentionally not provisioned by the accelerator.
  # Use IAM Identity Center (SSO) below for human identities.
  []

identityCenter:
  name: IdentityCenter
  delegatedAdminAccount: Audit
  identityCenterPermissionSets:
    - name: AdministratorAccess
      policies:
        awsManaged:
          - arn:aws:iam::aws:policy/AdministratorAccess
      sessionDuration: 60
    - name: PowerUserAccess
      policies:
        awsManaged:
          - arn:aws:iam::aws:policy/PowerUserAccess
      sessionDuration: 480
    - name: ReadOnlyAccess
      policies:
        awsManaged:
          - arn:aws:iam::aws:policy/ReadOnlyAccess
      sessionDuration: 480
    - name: BillingAccess
      policies:
        awsManaged:
          - arn:aws:iam::aws:policy/job-function/Billing
      sessionDuration: 240
    - name: SecurityAuditAccess
      policies:
        awsManaged:
          - arn:aws:iam::aws:policy/SecurityAudit
          - arn:aws:iam::aws:policy/ReadOnlyAccess
      sessionDuration: 480
  identityCenterAssignments:
    # No assignments generated: the landing zone cannot reference IdP users or
    # groups that do not exist until you connect your identity source. Until this
    # is populated, NO ONE can sign in to any account via SSO. After connecting
    # your IdP / creating groups, add entries here (or use the console or
    # `aws sso-admin create-account-assignment`) — see DEPLOYMENT_GUIDE.md. Example:
    #   - name: Admins
    #     permissionSetName: AdministratorAccess
    #     principalType: GROUP
    #     principalId: <group-id-or-name>
    #     deploymentTargets:
    #       accounts:
    #         - Management
    []
